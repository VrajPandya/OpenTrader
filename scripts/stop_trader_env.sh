#!/bin/bash

# Note: Try running the script from the project root directory
echo "if there are errors, try running the script from the project root directory"

# Stop the mongo server
# kill mongod jack hammer
killall -9 mongod
